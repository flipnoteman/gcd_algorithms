How to run the Program?

You can run the program by running the executable either through a console window:
  -navigating to the directory via cmd prompt and running ./PA-1-Executable
Or you can click on the executable file itself